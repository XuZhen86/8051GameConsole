#ifndef PUSHBUTTONCONFIG_H_
#define PUSHBUTTONCONFIG_H_

enum PUSHBUTTON_CONFIG{
    TRIAL_COUNT=8,          // Min trials to take before determine the key
    TOLERANCE_NUMBER=4,     // Tolerance for number keys
    TOLERANCE_DIRECTION=4   // Tolerance for direction keys
};

#endif
